# API ключ для доступа к OpenAI (обязательно!)
# Если используется ProxyAPI, укажите ключ от прокси-сервиса
OPENAI_API_KEY=your_actual_api_key_here

# Base URL для ProxyAPI (опционально)
# Если используется ProxyAPI, укажите URL:
# OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
OPENAI_BASE_URL=

# Модель OpenAI (по умолчанию gpt-5-mini)
# Стандартные модели OpenAI: gpt-5-mini, gpt-4o-mini, gpt-3.5-turbo, gpt-4o, gpt-4-turbo
# Для ProxyAPI могут быть доступны дополнительные модели
# Проверьте документацию вашего ProxyAPI провайдера
OPENAI_MODEL=gpt-5-mini

# Модель для эмбеддингов (используется в bot_long_memory.py)
# text-embedding-3-small - быстрая и дешёвая (по умолчанию)
# text-embedding-3-large - более точная, но дороже
EMBEDDING_MODEL=text-embedding-3-small

# Temperature - креативность ответов (0.0-1.0)
# 0.0-0.3: детерминированные ответы
# 0.4-0.6: сбалансированный подход
# 0.7-0.9: креативные ответы
# ВАЖНО: gpt-5-mini не поддерживает temperature (игнорируется автоматически)
OPENAI_TEMPERATURE=0.7

# Максимальное количество токенов в ответе
# 1000 токенов ≈ 750 слов
# Рекомендуется 1000-2000 для большинства задач
# Для gpt-5-mini и новых моделей используется max_completion_tokens
OPENAI_MAX_COMPLETION_TOKENS=2000
# Для совместимости со старыми моделями (опционально):
# OPENAI_MAX_TOKENS=2000

# Токен Telegram бота (для генератора ботов)
# Получите токен у @BotFather в Telegram
BOT_TOKEN=your_telegram_bot_token_here

# Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# DEBUG - максимум информации (для отладки)
# INFO - основная информация о работе (рекомендуется)
# WARNING - только предупреждения и ошибки
# ERROR - только ошибки
LOG_LEVEL=INFO

# ============================================
# ПРИМЕРЫ КОНФИГУРАЦИЙ ДЛЯ РАЗНЫХ ПРОВАЙДЕРОВ
# ============================================

# Стандартный OpenAI API:
# OPENAI_API_KEY=sk-proj-xxxxx
# OPENAI_BASE_URL=
# OPENAI_MODEL=gpt-5-mini

# ProxyAPI.ru:
# OPENAI_API_KEY=ваш_ключ_proxyapi
# OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
# OPENAI_MODEL=gpt-5-mini

# OpenRouter.ai:
# OPENAI_API_KEY=sk-or-xxxxx
# OPENAI_BASE_URL=https://openrouter.ai/api/v1
# OPENAI_MODEL=openai/gpt-5-mini

# При ошибках 500:
# 1. Проверьте правильность API ключа
# 2. Убедитесь, что модель доступна для вашего провайдера
# 3. Попробуйте другую модель (например, gpt-3.5-turbo)
# 4. Проверьте баланс аккаунта
# 5. Установите LOG_LEVEL=DEBUG для детальной диагностики
